# INTERACTION_TRANSPORT_BRIDGE_V1

This layer binds human-facing interaction continuity to deterministic governed transport continuity.

It preserves explicit references for interaction session, governed session, execution gate, provider invocation, bounded runtime, result capture, and response return.

Normalization is mechanical only: preserve bounded output, classify result state, preserve evidence, and fail closed when lineage is incomplete.
