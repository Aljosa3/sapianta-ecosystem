# REAL_INTERACTION_INGRESS_EGRESS_ADAPTER_V1

This adapter loads one deterministic local JSON ingress artifact, binds it to governed interaction transport continuity, and exports one deterministic local JSON egress artifact.

It is not a watcher, daemon, API, frontend, or autonomous runtime.
