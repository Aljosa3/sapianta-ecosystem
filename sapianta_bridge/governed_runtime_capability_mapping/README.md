# Governed Runtime Capability Mapping V1

This package defines the first governed executable capability realization layer.

The operation envelope says which bounded operation is allowed. Capability mapping says which exact deterministic executable primitive that operation may become. Mappings are static, replay-visible, and fail closed; executors are not invented dynamically.
