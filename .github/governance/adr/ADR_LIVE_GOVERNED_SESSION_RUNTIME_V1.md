# ADR: LIVE_GOVERNED_SESSION_RUNTIME_V1

## Status

Accepted.

## Decision

Add deterministic persistent governed runtime identity so multiple explicit interaction turns attach to the same session runtime ID. This removes manual runtime reconstruction without adding hidden memory, provider state trust, or autonomous infrastructure.
